# Cisco Project CodeGuard Overlays

This folder is reserved for Cisco-specific Project CodeGuard overlays.

Principles:

- keep upstream Project CodeGuard content intact,
- document Cisco-specific deltas explicitly,
- avoid silently changing upstream severity without a recorded reason,
- validate overlays on the demo repos before using them as defaults.
