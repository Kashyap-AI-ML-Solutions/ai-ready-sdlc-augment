---
description: Optional repo-local Auggie override for remediation work through the ADLC harness.
---

# ADLC Remediation Harness Local Override

This local command is optional. Route remediation through the installed plugin command `/ai-ready-sdlc:steer-adlc-harness` with task type `remediation`.

Before code changes, create `.adlc/plans/<YYYY-MM-DD>_<task-slug>/` with plan, tests, evals, work items, and proof requirements.
